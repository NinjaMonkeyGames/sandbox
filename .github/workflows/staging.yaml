# .github/workflows/staging.yaml

name: 🚦 Staging Pipeline

on:
  workflow_call:
    inputs:
      branch:
        required: true
        type: string

jobs:
  deploy_to_staging:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Deploy to Staging Environment
        run: echo "--- Deploying branch ${{ inputs.branch }} to Staging ---"