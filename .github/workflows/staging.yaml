name: 🧪 Staging Deployment

on:
  workflow_call:
    inputs:
      branch_name:
        required: true
        type: string

jobs:
  acknowledge_environment:
    runs-on: ubuntu-latest
    steps:
      - name: Acknowledge Staging Environment
        run: |
          echo "==================================================="
          echo "  DEPLOYMENT ACKNOWLEDGED: STAGING"
          echo "  Branch Triggered: ${{ inputs.branch_name }}"
          echo "==================================================="