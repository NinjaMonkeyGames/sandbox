name: Smoke Tests Utility Workflow

on:
  workflow_call:

jobs:
  run_smoke_tests:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      - name: 💨 Run Duplicate Commit Title Check Script
        # This step executes your specific shell script
        run: |
          echo "################################################"
          echo "## SMOKE TESTS WORKFLOW CALLED SUCCESSFULLY! ##"
          echo "################################################"
          echo "Executing script: duplicate-commit-title.sh"
          
          # Assuming the script is in a 'scripts' directory or the repository root
          # Adjust the path (e.g., ./scripts/duplicate-commit-title.sh) if necessary
          ./duplicate-commit-title.sh