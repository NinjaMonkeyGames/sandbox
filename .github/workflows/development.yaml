name: Development CI

on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development

jobs:
  # --- COMMON LINTING JOBS ---
  
  lint-commit:
    uses: ./.github/workflows/commitlint.yaml 
    # The 'secrets' block is REMOVED entirely
      
  lint-markdown:
    uses: ./.github/workflows/markdownlint.yaml
    # The 'secrets' block is REMOVED entirely

  # --- DEVELOPMENT SPECIFIC JOBS ---
  build-test-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Run Dev-Specific Build and Tests
        run: |
          echo "Running build and tests for Development environment..."