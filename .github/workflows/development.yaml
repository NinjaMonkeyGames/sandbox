name: Development

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  linting:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run markdown lint
        run: |
          echo "Markdown lint has been called."
          # Here you would normally run markdownlint commands

      - name: Run commit lint
        run: |
          echo "Commit lint has been called."
          # Here you would normally run commitlint commands
