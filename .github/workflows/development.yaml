# .github/workflows/development.yaml

name: 🚀 Development Pipeline

on:
  workflow_call:
    inputs:
      sha:
        required: true
        type: string
        
jobs:
  deploy_prod:
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: Deploy to Development
        run: echo "Deploying commit ${{ inputs.sha }} to development..."