name: Markdown Lint

on:
  push:
    branches:
      - '*'  # Run on all branch pushes
  pull_request:
    branches:
      - '*'  # Run on all PRs

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run markdown lint
        run: |
          echo "Markdown lint has been called."
          # Insert actual markdown linting command here, like `markdownlint .`
