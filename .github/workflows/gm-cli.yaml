name: gm-cli

on:
  workflow_call:
jobs:
  build-test:
    runs-on: ubuntu-latest
    container:
      image: monkeyknuckles/gm-cli
    steps:
      - uses: actions/checkout@v6

      #- name: Run cd
      #  run: cd grid-utility-professional

      - name: Run gm-cli
        run: gm-cli compile
