name: gm-cli

on:
  push:
    branches:
      - 'feature/**'

jobs:
  build-test:
    runs-on: ubuntu-latest
    container:
      image: monkeyknuckles/gm-cli
    steps:
      - uses: actions/checkout@v6

      - name: Run gm-cli
        run: gm-cli compile
