# .github/workflows/release.yaml

name: 🏷️ Release Pipeline

on:
  workflow_call:
    inputs:
      tag:
        required: true
        type: string

jobs:
  create_release:
    runs-on: ubuntu-latest
    steps:
      - name: Publish Release Notes
        run: echo "Creating GitHub release for tag ${{ inputs.tag }}..."