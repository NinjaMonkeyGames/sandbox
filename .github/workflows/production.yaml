name: Production CI

on:
  push:
    branches:
      - master # Updated to master
  pull_request:
    branches:
      - master # Updated to master

jobs:
  # --- COMMON LINTING JOBS ---
  lint-commit:
    uses: ./.github/workflows/commitlint.yaml
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
  lint-markdown:
    uses: ./.github/workflows/markdownlint.yaml
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # --- PRODUCTION SPECIFIC JOBS ---
  deploy-production:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Run Production Deployment
        run: |
          echo "Deploying to Production environment..."
          # Add your production-specific deployment steps here