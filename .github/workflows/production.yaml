# Logic for the master Branch
name: Production Deployment Workflow

on:
  workflow_call: # <-- CORRECT: Waits to be called by ci.yaml

jobs:
  production_deploy:
    runs-on: ubuntu-latest
    environment: production 

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: 🔒 Report Successful Production Call
        run: |
          echo "######################################################"
          echo "## PRODUCTION WORKFLOW (master Branch) CALLED! ##"
          echo "######################################################"
          echo "Triggered by branch: ${{ github.ref_name }}"
          echo "Preparing for live deployment to the PRODUCTION environment."