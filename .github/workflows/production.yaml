# .github/workflows/production.yaml

name: 🚀 Production Pipeline

on:
  workflow_call:
    inputs:
      sha:
        required: true
        type: string
        
jobs:
  deploy_prod:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Deploy to Production
        run: echo "Deploying commit ${{ inputs.sha }} to production..."